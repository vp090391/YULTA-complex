let initialState = {
    message: "Hello, I`m from state of About Laboratory",
};

const aboutLaboratoryReducer = (state = initialState) => {
    return state;
};

export default aboutLaboratoryReducer;