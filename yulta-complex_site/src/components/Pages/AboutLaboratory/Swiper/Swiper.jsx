import React from 'react';
import s from './Swiper.module.scss'

const Swiper = (props) => {
    let state = props.state;
    return (
        <>
            Swiper
        </>
    )
};

export default Swiper;